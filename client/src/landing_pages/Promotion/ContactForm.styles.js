import {makeStyles} from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
  error: {
    color: 'red',
  },
  callingYou: {
    fontWeight: 'bold',
    fontFamily: 'Roboto'
  }
}));

export default useStyles;
