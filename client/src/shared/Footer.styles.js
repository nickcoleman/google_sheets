import {makeStyles} from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
  container: {
    margin: '50px 0',
  },
  imageBox: {
    margin: '20px auto',
    maxWidth: 350,
  },
  media: {
    width: '90%',
    height: 'auto',
  },
  content: {},
}));

export default useStyles;
