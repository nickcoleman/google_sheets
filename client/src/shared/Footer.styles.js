import {makeStyles} from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
  container: {
    margin: '50px 0',
  },
  media: {
    width: '90%',
    height: 'auto',
  }
}));

export default useStyles;
